spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oopay?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: ${DB_PASSWORD:your_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    database: 0
  rabbitmq:
    host: localhost
    port: 5672
    username: oopay
    password: oopay

logging:
  level:
    com.oopay: DEBUG
    org.springframework.web: DEBUG
